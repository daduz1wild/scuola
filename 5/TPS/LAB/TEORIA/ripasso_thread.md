il concetto di thread
differenza user trhead e daemon thread
la parte riguardante la terminazione dei processi
attesa dei processi(wait(metodo statico della classe thread)
cosa intendo per fork(costrutto) cosa intendo per join
creazione di thread in java(quali sono le modalita) o interface runnable o classe thread con relative differenze )
costrutto syncronized per avere piu thread sincornizzati

public class Foglio la risorsa condivisa che ha bisogno di emtodi getcontenuto set add 
il thread che i questo caso e  printer  che la riusorsa dichiarata all'interno 
con il getF set F che modoifca o ritorna la risorsa

nella classe del thread ovviamentre c'è il metodo run che è in override perche è gia imple,mentata nella classe Thread o intrerface runnable

essendo che dobbiamo gestire l'accesso alla risorsa

Syncronyzed(f){

}

regola l'accesso alla risorsa bloccando l'accesso agli altri thread quando la risorsa è gia occupata mettiamo tutto quello che permette di accedere alla risorsa condivisa con pi thread tra le graffe del syncrlonized


gestione delle eccezioni con i thread(da mettere nel syncronized anche includendo l'handler )

Thread.currentThread.getName()

la wait rimane finche non viene chiamato notifie controlla a cosa serve

alla fine del syncronized c'è la wait

user thread non vinvolati alla jvm priorita maggiore e sono thread creati dal 

daemon thread minore priorita eseguiti in background come il garbage collector che si occupa di eliminare variabili inutilizzate e fare pulizia di sistema

area di memoria esclusiva del thread per ogni thread in cui ha le sue variabili esclusive
