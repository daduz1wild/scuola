io ho una rete accendo la macchina l'amministartore di rete non la configura, quindi mando una richiesta in rete al DHCP che configura automaticamente i parametri dik rete, (subnet mask, indirizzo ip, ,gateway, dns)


## 🧩 DHCP — Dynamic Host Configuration Protocol

Il **DHCP** (non HCCP) è un **protocollo di rete** che serve per **assegnare automaticamente gli indirizzi IP e altre informazioni di rete** ai dispositivi collegati.  
In parole semplici: è ciò che **configura in automatico il computer quando ti connetti a una rete** (Wi-Fi, LAN, ecc.).

---

## ⚙️ Cosa fa il DHCP

Quando colleghi un dispositivo (PC, smartphone, stampante, ecc.) a una rete, succede questo:

1. **Il dispositivo chiede un indirizzo IP**  
    → “Ehi, qualcuno può darmi un indirizzo IP per entrare in rete?”
    
2. **Il server DHCP (di solito il router)** risponde  
    → “Certo! Ti assegno l’indirizzo 192.168.1.5, la subnet mask 255.255.255.0, il gateway 192.168.1.1 e il DNS 8.8.8.8.”
    
3. **Il dispositivo riceve e salva la configurazione automaticamente**  
    → Così può comunicare con gli altri dispositivi e con Internet senza che tu debba impostare nulla manualmente.
    

---

## 🧮 Il DHCP assegna automaticamente:

|Parametro|Significato|
|---|---|
|**Indirizzo IP**|L’indirizzo univoco del dispositivo nella rete|
|**Subnet mask**|Serve a capire quali dispositivi appartengono alla stessa rete|
|**Gateway predefinito**|L’indirizzo del router (l’uscita verso Internet)|
|**Server DNS**|Per tradurre i nomi dei siti in indirizzi IP|

---

## 🏠 Esempio pratico

Quando ti connetti al Wi-Fi di casa:

- Il tuo PC chiede un IP → DHCP gli dà `192.168.1.10`
    
- Gli dice anche:
    
    - Subnet mask: `255.255.255.0`
        
    - Gateway: `192.168.1.1`
        
    - DNS: `8.8.8.8`
        
- Ora puoi navigare senza configurare nulla manualmente ✅
    

---

## 🧠 In breve

|Cosa fa|Chi lo fa|
|---|---|
|Assegna automaticamente l’indirizzo IP|**DHCP**|
|Traduce i nomi dei siti in IP|**DNS**|
|Gestisce la connessione fisica|**MAC address / scheda di rete**|
|Permette di testare la connessione|**Ping**|

---

## ⚠️ Attenzione

- Se il DHCP è **disattivato** nel router, i dispositivi **non ricevono automaticamente un IP**, e quindi **non possono connettersi a Internet** finché non lo imposti manualmente.
    
- DHCP ≠ DNS → sono due cose diverse ma lavorano insieme:
    
    - **DHCP** ti dà l’IP
        
    - **DNS** ti dice a chi appartiene un certo nome (es. [www.google.com](http://www.google.com/))
        

---

Vuoi che ti faccia anche **uno schema visivo** con **DHCP, DNS, IP, gateway e ping** collegati tra loro (utile per studiare per un’interrogazione di reti)?