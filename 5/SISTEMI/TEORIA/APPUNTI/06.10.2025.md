ipconfig ci dice le caratteristiche della nostra rete
gateway, subnet mask, indirizzo di rete, MAC address(scheda rete)

ping=a **funzione `ping`** in **sistemi e reti** serve per **verificare la connettività** tra due dispositivi all’interno di una rete, ad esempio tra il tuo computer e un altro host (come un server o un router).  
In pratica, ti permette di **sapere se un dispositivo è raggiungibile** e **quanto tempo impiega** un pacchetto di dati per andare e tornare (latenza).

differenza tra indirizzo MAC e indirizzo IP

ping morrow

quando inserisco un url nella barra dui ricerca viene aperto il file host da cui si trovera dove coincided il dns, poi da quel che ho capito se c'è il gateway il pacchetto non puo fare la richiesta per risolvere il dns

DNS, cos'è e come funziona