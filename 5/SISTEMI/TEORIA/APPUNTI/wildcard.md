wildcard è un filtro che serve per indicare quanto è grande un blocco di indirizziper mettere delle regole un gruppo di indirizzi al contrario della subnet mask serve per sapere se un indirizzo è interno o esterno.
DMZ è quella zona della mia rete che non è protetta e non voglio che mi sia inviato traffico di rete, e dove pubblico dei server .

calcolo per 172.16.0.5 con wildcard 0.0.255.3 chiedi a chatù
a cosa serve fare l'opposto della wildcard e a cosa la and come ma lo si fa per ottetto 3 ottetto 4 e poi decimale ad esempio in una rete di classe B



INDIRIZZO MAC
OUI identifica il produttore della scheda della rete

ipconfig /displaydns per vedere quali sono i dns presenti nella tabella   ipconfig /flushdns per cancellare i dns

proxy gestisce gli accessi al server da parte di un client.
ssl evoluzione del tls