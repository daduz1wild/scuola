primitivi 
sempre istanziate a compile time
.int
.float
.boolean
.char

riferimento
sono dei puntatori che fanno riferimento ad un oggetto che deve essere ancora creato.sonos empre istanziate  a run time.
.classi
.array

scanner input=new scanner(system,in);

 con la new si va ad istanziare nell'heap una zona di memoria alla quale punto l'input

le stringhe funzionano come dei puntatori

chiedia chat gpt d spiegarti come funziona nello stack e heap questa funzione sc=new Scanner

avendo il garbage collector noi avremo la new ma non avremo mai la close 

tra i metodi di una classe abbiamo quelli costruttori che

il costruttore ha lo stesso nome della classe e viene utilizzato tramite dei parametri ed esso sa che tipo di lavoro deve fare sul parametro ricnoscendo il tipo di esso  quindi ci sono più costrttori della stasse classe.
il costruttore è il metodo che inizializza gli oggetti della classe 

non ci sono prototipi in J
ma quando definiamo una procedura deve essere static

solamente classi ed array sono di riferimento

solo primitivi sono di tipo by value


in alcuni casi non c'è bisogno di allocare la memoria qwuando si ha una stringa infatti ci pensano i costruttori a farlo.