il proxy è un server itermediario strategico che ci permette di avere grandi vantagggi, puo essere usato per caching usandolo per salvare una pagina in modo da non doverla ricaricare ogni volta, lo svataggio è che se un apagina è stata modificata su intenret, io non vedo le modifiche perche ho gia la pagina vecchia sulla cache e mi mostra qurella, quindi dovrei fare una ricerca manuale.
ùfiltraggio.: blocca l'acesso a determinati siti web, a scuola proxy c'è.


forward proxy: usato dai client per accedere a internet tramite il proxy
reverse proxy:posto davanti a uno o piu server interni per protezione, bilanciamento del carico SSL(si intend eil bilanciamento del traffico di rete)

i proxy nella ret offloading

proxy nasconde l'ip pubblico.

CONTROLLO DI ACCESSO: la proxy permette di salvare nei log le attiovita dell'utenza.

i l proxy solitamente viene posizionato tra gli host e il router/firewall:
- server fisico: installazione su hardware dedicato per messime prestazioni
- rete separataposizionamento in una zona di sicurezza isolata
PROXY VS FIREWALL
lavorano a livelli diversi infatti proxy livello 7(livello di applicazione) e l'altro è livello 8(livello di trasporto, quindi solo  alivello di pacchetti trasmessi)


la differnza tra antivirus e firewall è che uno antivirus da quello che ho capito devia il lavoro del firewall

di solito sul oc abbiamosd em pre una porta di ascolto e una di scrittura

i file per installar eroxy sulla machina dei clioent puoi usare file .pac



